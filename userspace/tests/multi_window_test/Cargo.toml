[package]
name = "multi_window_test"
version = "0.1.0"
edition = "2021"

[dependencies]
libpanda = { path = "../../libpanda" }
panda-abi = { path = "../../../panda-abi" }

[profile.dev]
panic = "abort"

[profile.release]
panic = "abort"
