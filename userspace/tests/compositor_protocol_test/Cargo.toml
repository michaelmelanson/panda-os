[package]
name = "compositor_protocol_test"
version.workspace = true
edition.workspace = true

[dependencies]
libpanda = { workspace = true }
compositor-protocol = { path = "../../compositor-protocol" }
